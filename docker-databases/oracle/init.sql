-- conectat ca SYS
ALTER SESSION SET CONTAINER = XEPDB1;

CREATE USER univuser IDENTIFIED BY password;
GRANT CREATE SESSION, CREATE TABLE, CREATE SEQUENCE TO univuser;
ALTER USER univuser DEFAULT TABLESPACE USERS;
ALTER USER univuser QUOTA UNLIMITED ON USERS;

CREATE TABLE COURSES (
                         id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                         name VARCHAR2(100) NOT NULL,
                         teacher_id INT
);

INSERT INTO COURSES (name, teacher_id) VALUES ('Math 101', 1);
INSERT INTO COURSES (name, teacher_id) VALUES ('Physics 101', 2);